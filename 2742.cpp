#include <iostream>

using namespace std;

int main()
{

    cin.tie(NULL);
    std::ios::sync_with_stdio(false);


    int iter;
    cin >> iter;

    for(int i = iter; i > 0; i--)
    {
        cout << i << "\n";
    }
}