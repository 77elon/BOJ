#include <iostream>
#include <algorithm>
#include <cmath>

using namespace std;


int main()
{
    cin.tie(NULL);
    std::ios::sync_with_stdio(false);

    int L = 0;
    cin >> L;

    int S[50];
    memset(S, 0, sizeof(S));

    for(int i = 0; i < L; i++)
    {
        cin >> S[i];
    }

}